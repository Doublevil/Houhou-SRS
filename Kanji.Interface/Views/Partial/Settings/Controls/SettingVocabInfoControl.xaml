<UserControl x:Class="Kanji.Interface.Controls.SettingVocabInfoControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <StackPanel>
        <CheckBox Content="Show vocab frequency in books" IsChecked="{Binding ShowBookRanking}" Margin="0 0 0 5" />
        <CheckBox Content="Show vocab frequency on Wikipedia" IsChecked="{Binding ShowWikipediaRank}" Margin="0 0 0 5" />
        <CheckBox Content="Show vocab JLPT level" IsChecked="{Binding ShowJlptLevel}" Margin="0 0 0 5" />
        <CheckBox Content="Show vocab WaniKani level" IsChecked="{Binding ShowWkLevel}" Margin="0 0 0 0" />
    </StackPanel>
</UserControl>
